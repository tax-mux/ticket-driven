# ジャーナル・タイミング

| タイミング | ジャーナル |
|-----------|------------|
| 精緻化（自動時） | `精緻化: {要約}`（refine 側。充足済みなら「更新なし」） |
| 分割 | `チケットを分割: ...`（split 側） |
| 着手 | `着手: {概要}`（実装チケット＝子優先） |
| テスト計画 | `テスト計画: {要約}`（新規作成・大幅補完時のみ） |
| テスト実行 | 完了ノートに含めてよい（`完了: ... — テスト: {結果}`） |
| リファクタ（予防） | `リファクタ: 予防 — {要約}`（大きめの分割・浅化時のみ） |
| リファクタ検討 | `リファクタ検討: {実施|見送り} — {要約}`（しきい値超過時のみ） |
| 完了 | `完了: {概要} — {検証結果}` |

進捗ジャーナルの連投は不要。詰まったときだけ追記。
