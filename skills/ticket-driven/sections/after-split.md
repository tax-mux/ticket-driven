# 分割後の作業順

1. 依存なし（先行）の子から着手する（粗い子のまま着手しない）
2. 子の着手前に `ticket-refine`（**漏れ拾い**。split が充足していれば更新スキップ）
3. 子のテスト範囲は子の完了条件に対応させる（親の計画をコピーしない）
4. 子を Resolved にするたびに親の `done_ratio` を更新する
5. すべての子が Resolved になったら親を Resolved にする（親に完了ノート）

粗い子の再分割は `ticket-split` の再分割手順に従う。
