# Dependencies
node_modules/

# Environment / secrets
.env
.env.*
!.env.example

# OpenCode local config (may contain API keys)
.opencode/opencode.jsonc

# Credentials / keys
*.pem
*.key
credentials.json
secrets.json
auth.json

# OS / editor
.DS_Store
*.swp
*~
